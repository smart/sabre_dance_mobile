<div id="main" class="plastic current">
  <div class="toolbar">
    <a class="back">Back</a>
	  <h1>Tours</h1>
	</div>

  <ul id="tours" title="Tours" selected="true">
	  <%@tours.each do |x|%>
	  <li><%=link_to "#{x.name}", :action => :show, :id => x.object%></li>
	  <%end%>
  </ul>
</div>