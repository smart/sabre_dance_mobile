<div id="venues" class="plastic current">
  <div class="toolbar">
    <a class="back">Back</a>
	  <h1>Venues</h1>
	</div>

  <ul>
	  <%@venues.each do |x|%>
      <li><%=link_to "#{x.name}", :action => :show, :id => x.object%></li>
    <%end%>
  </ul>
</div>